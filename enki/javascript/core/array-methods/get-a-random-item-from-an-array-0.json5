{
  __meta__: {
    author: 'AlexYancey',
    source: 'https://github.com/enkidevs/curriculum/blob/master/javascript/core/array-methods/get-a-random-item-from-an-array.md',
  },
  type: 'choose_sequence',
  lang: 'en',
  tags: [
    'enki',
    'how to',
  ],
  card: {
    question: '<p>How would you select a fruit at random\nfrom the array <code>fruits</code>?</p>\n<pre><code>var fruits =[oranges, apples, banana];\n\nvar index =\nMath.???(\n  Math.random() *\n    fruits.length);\n\nconsole.log("the random fruit is"\n  +fruits[index]);\n</code></pre>\n',
    answers: [
      {
        text: 'floor',
      },
      {
        text: 'ceil',
      },
      {
        text: 'wall',
      },
    ],
    comment: '<p>Given the array:</p>\n<pre><code>var food = ["Sushi", "Pizza", "Tacos"];\n</code></pre>\n<p>Select an item using <code>Math.random()</code> with a range of <code>food.length</code>.</p>\n<pre><code>var randIndex =\n  Math.floor(Math.random() * food.length);\n</code></pre>\n<p>Now, print out the result.</p>\n<pre><code>console.log("Let\'s get " + food[randIndex]);\n</code></pre>\n',
  },
}