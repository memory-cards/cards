{
  __meta__: {
    author: 'catalin',
    source: 'https://github.com/enkidevs/curriculum/blob/master/javascript/core/recipes-i/round-numbers-to-n-decimals.md',
  },
  type: 'choose_sequence',
  lang: 'en',
  tags: [
    'enki',
    'how to',
  ],
  card: {
    question: '<p>Complete the following code snippet:</p>\n<pre><code class="language-javascript">//convert the number to length 3\nconsole.log((12.4513).???(3);\n//convert the number to have length 3 \n//after the decimal point\nconsole.log((12.4513).???(3);\n</code></pre>\n',
    answers: [
      {
        text: 'toPrecision',
      },
      {
        text: 'toFixed',
      },
      {
        text: 'dec',
      },
      {
        text: 'length',
      },
    ],
    comment: '<p>Two methods that can be useful in rounding numbers.</p>\n<ul>\n<li><code>toFixed(n)</code>: argument provides the length after the decimal point</li>\n<li><code>toPrecision(n)</code>: argument provides the total length of the number</li>\n</ul>\n<p>Use <code>toFixed</code>:</p>\n<pre><code class="language-javascript">var num = 2.44534;\nconsole.log(num.toFixed(3)); // 2.445\n</code></pre>\n<p>Use <code>toPrecision</code>:</p>\n<pre><code class="language-javascript">var num = 444.3245;\nconsole.log(num.toPrecision(5)); //444.32\n</code></pre>\n',
  },
}