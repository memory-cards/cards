{
  __meta__: {
    author: 'rosielowther',
    source: 'https://github.com/enkidevs/curriculum/blob/master/javascript/core/functions-ii/iife-pattern.md',
  },
  type: 'choose_sequence',
  lang: 'en',
  tags: [
    'enki',
    'best practice',
    'introduction',
    'workout',
  ],
  card: {
    question: '<p>What\'s the syntax of an *<em>IIFE</em></p>\n<pre><code class="language-javascript">(??? () {\n}???);\n</code></pre>\n',
    answers: [
      {
        text: '?',
      },
      {
        text: 'function',
      },
      {
        text: '()',
      },
      {
        text: '{}',
      },
      {
        text: '(',
      },
      {
        text: ')',
      },
      {
        text: 'name',
      },
      {
        text: 'call',
      },
    ],
    comment: '<p>The <strong>Immediately Invoked Function Expression pattern</strong> (<strong>IIFE</strong> or "iffy") is a function expression (either named or anonymous) that is immediately called. </p>\n<p>The syntax is:</p>\n<pre><code>(function(){\n    // do something\n}());\n</code></pre>\n<p>It is an alternative to:</p>\n<pre><code>// do something\n</code></pre>\n<p>You can use self-executing functions to maintain a <strong>clean scope</strong>, as variables declared within the function are not available outside it.</p>\n<p>Two possible uses are:</p>\n<ul>\n<li>\n<p>To wrap the whole code in an IIFE and pass in the <code>window</code> object. This could be given an alias of <code>global</code>. Global public properties could then be made accessible.</p>\n</li>\n<li>\n<p>To produce an alias for <code>jQuery</code> to <code>$</code> to prevent conflicts with other frameworks.</p>\n</li>\n</ul>\n<p>For example:</p>\n<pre><code>(function(global){\n  function fun(){\n      return "Hello World!";\n  }\n  global.fun = fun;\n}(window)); // reference to window object\n</code></pre>\n',
  },
}