{
  __meta__: {
    author: 'rosielowther',
    source: 'https://github.com/enkidevs/curriculum/blob/master/javascript/core/functions-ii/functions-return-undefined-by-default.md',
  },
  type: 'choose_sequence',
  lang: 'en',
  tags: [
    'enki',
    'feature',
    'introduction',
    'workout',
  ],
  card: {
    question: '<p>Fill in the output of the following <code>foo</code> calls:</p>\n<pre><code class="language-javascript">function foo(num) {\n  if (num === 1) {\n    return;\n  }\n  if (num === 2) {\n    return \'Enki\';\n  }\n}\nfoo(1); // = ???\nfoo(2); // = ???\nfoo(3) === foo(1); // = ???\n</code></pre>\n',
    answers: [
      {
        text: 'undefined',
      },
      {
        text: 'Enki',
      },
      {
        text: 'true',
      },
      {
        text: 'null',
      },
      {
        text: 'false',
      },
      {
        text: 'NaN',
      },
    ],
    comment: "<p>JavaScript is a <strong>dynamic</strong> programming language which means that it is not necessary to define the return type of a function.</p>\n<p>If the function <code>return</code> statement is empty (or missing) the default value is <code>undefined</code>.</p>\n<p>For example:</p>\n<pre><code class=\"language-javascript\">function doSomething(num) {\n  if (num === 1) {\n    return;\n  }\n  if (num === 2) {\n    return 'Enki';\n  }\n}\n\ndoSomething(1) // undefined\ndoSomething(2) // 'Enki'\ndoSomething(3) // undefined\n</code></pre>\n",
  },
}