{
  __meta__: {
    author: 'NomaDube',
    source: 'https://github.com/enkidevs/curriculum/blob/master/javascript/core/functions-ii/function-constructor-vs-function-expression.md',
  },
  type: 'choose_options',
  lang: 'en',
  tags: [
    'enki',
    'feature',
    'introduction',
  ],
  card: {
    question: '<p>What is the following?</p>\n<pre><code class="language-javascript">var multiply = function(x, y) {\n   return x \n}\n</code></pre>\n<p>???</p>\n',
    answers: [
      {
        text: 'y;',
        correct: true,
      },
      {
        text: 'function expression of an anonymous function',
        correct: false,
      },
      {
        text: 'function defined with <code>function</code> constructor',
        correct: false,
      },
      {
        text: 'function expression of a <code>function</code> named <code>mult</code>',
        correct: false,
      },
      {
        text: 'all',
        correct: false,
      },
      {
        text: 'none',
        correct: false,
      },
    ],
    comment: '<p>The concepts of constructors, declaration and expressions are often misused. Here is the official terminology according to MDN:</p>\n<p>A function defined with the <code>function</code> <em>constructor</em> assigned to the variable <code>multiply</code>:</p>\n<pre><code>function multiply(x, y) {\n   return x * y;\n}\n</code></pre>\n<p>Function <em>expression</em> of an anonymous function assigned to the variable <code>multiply</code>:</p>\n<pre><code>var multiply = function(x, y) {\n   return x * y;\n}\n</code></pre>\n<p>Function <em>expression</em> of a <code>function</code> named <code>mult</code> assigned to the variable <code>multiply</code>:</p>\n<pre><code>var multiply = function mult(x, y) {\n   return x * y;\n};\n</code></pre>\n',
  },
}