{
  __meta__: {
    author: 'tommarshall',
    source: 'https://github.com/enkidevs/curriculum/blob/master/javascript/browser-apis/reading-writing/filereader-readasdataurl.md',
  },
  type: 'choose_sequence',
  lang: 'en',
  tags: [
    'enki',
    'feature',
  ],
  card: {
    question: "<p>Which FileReader's function is called after <code>readAsDataURL()</code> is completed?</p>\n<p>???</p>\n",
    answers: [
      {
        text: '`onload`',
      },
      {
        text: '`onend`',
      },
      {
        text: '`oncomplete`',
      },
      {
        text: '`readAsDataURL`',
      },
    ],
    comment: '<p>The <code>readAsDataURL()</code> method is used with file or blob objects. When the read operation is complete it returns a <em>data URL</em> with the files contents as base64 encoded string which can be access through <code>reader.result</code>.</p>\n<p>This functionality is often used to create thumbnail previews of uploaded image files.</p>\n<p>The HTML:</p>\n<pre><code class="language-html">&#x3C;input type="file" id="file"\n  onchange="triggerRead()"\n  style="display:block">\n&#x3C;img src="" id="wallpaper">\n</code></pre>\n<p>And the JavaScript:</p>\n<pre><code class="language-javascript">function triggerRead() {\n  var wallpaper =\n    document.getElementById(\'wallpaper\');\n  var file = document.getElementById(\'file\')\n    .files[0];\n  var reader  = new FileReader();\n\n  reader.onload = function(e) {\n    wallpaper.src = reader.result;\n  }\n\n  if(file) {\n    reader.readAsDataURL(file);\n  }\n}\n</code></pre>\n<p>Here, the variable <code>e</code> stands for <code>Event</code>, which is an interface that represents any event that takes place in the DOM (Document Object Model). Some of these events are user generated (e.g. keyboard or mouse events), whilst others are generated by some APIs (e.g. when a video is paused or when all updates have been installed).</p>\n',
  },
}