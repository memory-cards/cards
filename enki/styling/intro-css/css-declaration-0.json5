{
  __meta__: {
    author: 'mihaiberq',
    source: 'https://github.com/enkidevs/curriculum/blob/master/web/styling/intro-css/css-declaration.md',
  },
  type: 'choose_sequence',
  lang: 'en',
  tags: [
    'enki',
    'must-know',
  ],
  card: {
    question: '<p>Complete the following snippet with the general syntax for a CSS declaration:</p>\n<pre><code class="language-css">??? {\n  ??? ??? ???;\n}\n</code></pre>\n',
    answers: [
      {
        text: 'selector',
      },
      {
        text: 'property',
      },
      {
        text: ':',
      },
      {
        text: 'value',
      },
      {
        text: '=',
      },
      {
        text: 'background-decoration',
      },
      {
        text: 'red',
      },
    ],
    comment: '<p>A CSS file is made up of <em>selectors</em> (a way of pointing to the HTML elements to be changed) and <em>declarations</em> (a property and value pair).</p>\n<p>The syntax for a CSS <em>declaration</em> is:</p>\n<pre><code class="language-css">background-color: red\n</code></pre>\n<p>The property we are looking to change is <code>background-color</code>. The <code>:</code> separates the two entities, and <code>red</code> is the new property value.</p>\n<p>Every <em>declaration</em> has to be located inside a <em>block</em>:</p>\n<pre><code class="language-css">{\n  background-color: red\n}\n</code></pre>\n<p>Multiple declaration can be grouped inside a block. In this case, semicolons, <code>;</code> are required after each declaration:</p>\n<pre><code class="language-css">{\n  background-color: red;\n  text-decoration: underline;\n}\n</code></pre>\n<p>The above snippets made some changes, but CSS doesn\'t know which elements they are referring to. <em>Selectors</em> are used to specify those elements:</p>\n<pre><code class="language-css">p{\n  background-color: red;\n  text-decoration: underline;\n}\n</code></pre>\n<p><code>p</code> is a type selector. Whatever CSS rule you apply to <code>p</code> in this example, it will affect all instances of <code>&#x3C;p></code> elements in the HTML document.</p>\n',
  },
}